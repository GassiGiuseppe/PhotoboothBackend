# Runtime
APP_ENV=local
# <dialect>+<driver>://<user>:<password>@<host>:<port>/<database>
DATABASE_URL=postgresql+asyncpg://app:secret@local_db:5432/photoindex
LOCAL_DATA_DIR=/data
STORAGE_BACKEND=local
# Per GCS quando servirà
# GCS_BUCKET=your-bucket
# GCS_PREFIX=photos

# DB (compose)
POSTGRES_DB=photoindex
POSTGRES_USER=app
POSTGRES_PASSWORD=secret

# GCS
GCS_BUCKET=nome-del-tuo-bucket
GCS_PREFIX=photos                   # opzionale (cartella/prefix nel bucket)
GOOGLE_APPLICATION_CREDENTIALS=/var/secrets/gcp/key.json